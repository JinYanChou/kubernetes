apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
  namespace: test-namespace
  labels:
    app: mysql
data:
  MYSQL_ROOT_PASSWORD: cathaylife
  # MYSQL_DATABASE: DBAB
  # MYSQL_USER: dbab
  # MYSQL_PASSWORD: dbab